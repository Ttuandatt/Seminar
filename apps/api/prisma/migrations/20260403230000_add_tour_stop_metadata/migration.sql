ALTER TABLE "tour_pois"
  ADD COLUMN IF NOT EXISTS "titleOverride" TEXT,
  ADD COLUMN IF NOT EXISTS "descriptionOverride" TEXT,
  ADD COLUMN IF NOT EXISTS "customIntro" TEXT,
  ADD COLUMN IF NOT EXISTS "estimatedStayMinutes" INTEGER,
  ADD COLUMN IF NOT EXISTS "transitionNote" TEXT,
  ADD COLUMN IF NOT EXISTS "isRequired" BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS "unlockRule" TEXT,
  ADD COLUMN IF NOT EXISTS "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  ADD COLUMN IF NOT EXISTS "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP;

CREATE UNIQUE INDEX IF NOT EXISTS "tour_pois_tourId_orderIndex_key" ON "tour_pois"("tourId", "orderIndex");
