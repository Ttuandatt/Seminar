# API URL — trỏ đến backend đang chạy
# Đổi thành LAN IP của máy chạy backend (không dùng localhost — điện thoại không hiểu localhost)
# Tìm IP: ipconfig (Windows) hoặc ifconfig (macOS/Linux)
EXPO_PUBLIC_API_URL=http://192.168.1.x:3000
