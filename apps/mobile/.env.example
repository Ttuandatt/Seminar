# API URL — optional override
# Auto-detected from Expo Go's LAN IP if not set (recommended for dev)
# Only uncomment if auto-detect doesn't work for your setup
# EXPO_PUBLIC_API_URL=http://192.168.1.x:3000/api/v1
