# Environment
.env
node_modules/
uploads/
dist/
.worktrees/

# Archives & large binaries (do not commit)
*.rar
*.zip
*.7z
*.tar
*.tar.gz
*.tgz

# Sensitive / hard-coded scratch DB scripts
apps/api/scratch/

# PDFs in repo root (generated artifacts, attach to PR/Drive instead)
/*.pdf
